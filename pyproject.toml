[tool.ruff]
target-version = "py312"
line-length = 110
src = ["dags", "plugins", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.isort]
known-first-party = ["common"]

[tool.pytest.ini_options]
pythonpath = ["dags", "plugins"]
testpaths = ["tests"]
